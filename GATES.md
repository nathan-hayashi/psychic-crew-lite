# GATES.md — Gate Ledger

Format: gate · ISO · demo_result · stress_result · operator_token_line.
Verdicts are exactly PASS | FAIL | ESCALATE. PASS requires zero open P0.

| Gate | ISO (UTC) | Demo result | Stress result | Operator token line |
|---|---|---|---|---|
| L0 | 2026-08-21T15:11:36Z | SCAFFOLD. 18 files on branch dev at 997651f: models.config.json with capability classes, apply-models.sh resolving class -> alias -> id, four agent bodies at 22-36 lines, four rules (security.md and fallback-protocol.md MIRRORED byte-identical from the parent, model-policy.md ADAPTED for classes, release-protocol.md NEW replacing the arbiter protocol), line-ending policy, ignore rules, README, ledgers, and the §7.1 sync correlation map. apply-models stamps 4 agents, mode=alias. check-sync **15 PASS / 0 FAIL / 0 REVIEW**. All scripts parse. No absolute home prefix and no forbidden vendor name in any tracked file. Parent repo unchanged and green at 158 PASS / 0 FAIL | CONTROLS EXECUTED, not asserted. **Class indirection closed rather than inherited**: a forbidden model reachable through a class whose own declaration reads "deep", with no forbidden string visible at the agent, is caught at the RESOLVED value — exit 2, and the run refuses before stamping; a forbidden id in the pinned block is caught in alias mode too. **The sync map is enforced**: planting an edit in a MIRRORED file fails by name, and a DROPPED row naming a lite path fails with the correction. It caught .gitattributes diverging within minutes of the map declaring it MIRRORED, because a Lite-specific comment had been written into it — mirrored means mirrored, and it now carries the parent's bytes. SCOPE CALL DISCLOSED: apply-models.sh is a script and L0's scope names config, agents, rules and ignore rules — included because LC-4 is "one file, ONE COMMAND" and config with no command to apply it is half a constraint. GATE GRAMMAR CORRECTED: the plan's §3 and §9 contradicted each other on whether this token opens or closes L0; §9 was wrong, and a token issued before the work exists now authorises the start and never the close PUBLISHED at github.com/nathan-hayashi/psychic-crew-lite — PUBLIC, default branch dev, no main, no license, issues/wiki/projects on: every setting read off the parent and matched rather than assumed. All 18 tracked files were read end to end before publication, since publishing distributes them; no secret, credential or absolute machine path in any of them | **APPROVED** `APPROVE GATE-L0` @ 2026-08-21T15:30:47Z |
| L1 | 2026-08-21T16:03:52Z | ENFORCEMENT. Six wired hooks plus the shared preamble, .claude/settings.json, and scripts/validate-lite.sh. validate-lite **24 PASS / 1 SKIP / 0 FAIL**, eleven assertions behavioural under a mktemp root so no case touches the live trail. check-sync **36 PASS / 0 FAIL** across a map grown 14 -> 34 rows. Hooks are bound to settings.json in BOTH directions from the first commit that has them — carrying C-26 forward as a design input rather than inheriting it, since the parent's equivalent check has never policed its own hooks directory | CONTROLS 12/12. The one that earned its keep: my LC-2 scan PASSED WHILE RESOLVING NOTHING — inside a jq to_entries pipe the dot is the entry, not the root, so every forbidden-substring comparison ran against an empty list. The static check said clean; the behavioural case that feeds model-guard a poisoned config said allowed. Root captured in both places and a vacuity guard added asserting one resolved model per agent. Twelfth instance of this family and the first caught behaviourally rather than by reading. DECIDED, not defaulted: security.md moved MIRRORED -> ADAPTED because byte-identity shipped references to an arbiter this build does not have, while the SEVERITY TABLE stays pinned byte-identical — two builds that disagree about `crit` cannot exchange findings. The L0 register framed that as a binary and it was not. ADDED relation introduced, closing a hole the map's own rule forbade: every relation required a parent path, so release-protocol.md had been undeclared drift since L0 by the definition of the map meant to declare it | awaiting `APPROVE GATE-L1` |
